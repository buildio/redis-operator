apiVersion: databases.spotahome.com/v1
kind: RedisFailover
metadata:
  name: redisfailover
spec:
  sentinel:
    enabled: true
    replicas: 3
    image: redis:7.2-alpine
    imagePullPolicy: IfNotPresent
  redis:
    replicas: 3
    image: redis:7.2-alpine
    imagePullPolicy: IfNotPresent
