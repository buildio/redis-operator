apiVersion: v1
name: redis-operator
version: 4.1.0
# appVersion matches the Docker image tag (no leading "v")
# Chart version, appVersion, and image tag are all the same: 4.1.0
appVersion: "4.1.0"
description: A Helm chart for the Redis Operator
home: https://github.com/saremox/redis-operator
keywords:
  - "golang"
  - "operator"
  - "cluster"
sources:
  - https://github.com/saremox/redis-operator
kubeVersion: ">=1.32.0-0"
